# Core web stack
Flask==3.1.3
Werkzeug==3.1.8
flask-cors==6.0.5
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.41
marshmallow==4.3.1
python-dotenv==1.2.1

# Data and market access
pandas==2.3.3
numpy==2.3.4
yfinance==0.2.59
requests==2.32.5

# Forecasting.
# Optional: the API starts without it and returns 503 from the forecasting
# endpoints only. Install to enable /api/forecast. Use the GPU build
# (`tensorflow`) instead if CUDA is available.
tensorflow-cpu==2.21.0

# Production server
gunicorn==23.0.0; sys_platform != "win32"
waitress==3.0.2; sys_platform == "win32"
